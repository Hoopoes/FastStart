# Supported values:
#   json   -> structured JSON logs (recommended for production)
#   logfmt -> key=value structured logs (lightweight, human-readable)
LOG_FORMAT=logfmt

ROOT_PATH=/hoopoes

# Auth
AUTH_SECRET_KEY=your-very-strong-secret-key
AUTH_ALGORITHM=HS256
AUTH_ACCESS_TOKEN_EXPIRE_MINUTES=15
